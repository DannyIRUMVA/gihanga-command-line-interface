# Gihanga project resources

This folder is for project-local Gihanga resources.

Recommended uses:

- `skills/` for project ubumenyi
- `prompts/` for reusable prompt templates
- `extensions/` for trusted project extensions
- `themes/` for project themes

Keep this folder clean. Do not store secrets, API keys, credentials, or personal tokens here.
